apiVersion: v1
kind: Service
metadata:
  name: servicio-externo-nodo
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31080 # Forzamos este puerto en el Nodo
  selector:
    app: demo-networking
