name: hello-world
on:
  push:
    branches:
      - integration
  pull_request:
    branches:
      - main
jobs:
  hola:
    runs-on: ubuntu-latest
    steps:
      - name: hello-world
        run: echo "Hello-world"

      - name: touch file
        run: touch test.txt

      - name: set
        run: echo "hello integration" >> test.txt

      - name: show
        run: cat test.txt

  otro:
    runs-on: ubuntu-latest
    steps:
      - name: LS
        run: ls -la
